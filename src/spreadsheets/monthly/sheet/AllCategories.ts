class AllCategories extends BaseSheetHandler {
  private getColumnIndexForCategory(categoryName: string) {
    const columnIndex = this.spreadSheetConfig.extra.categoriesColumns[]
    // TODO: CONTINUE HERE
  }
}